# Base URL of the AI Operations Control Tower API (../app/api.py). Only
# needed if you're running the API somewhere other than the default --
# src/api.ts falls back to http://localhost:8001 if this isn't set.
VITE_API_BASE_URL=http://localhost:8001
