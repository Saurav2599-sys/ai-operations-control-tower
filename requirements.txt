# Web framework + server
fastapi
uvicorn[standard]

# ORM + Postgres driver. psycopg2-binary trades a bit of install-time
# convenience for not needing Postgres dev headers on your machine -- fine
# for local/portfolio use; a production build would use psycopg2 built
# from source instead.
sqlalchemy>=2.0
psycopg2-binary

pydantic>=2.0
python-dotenv

# Phase 2: order-to-employee assignment. CP-SAT is the constraint solver
# used in app/assignment.py's optimized_assign().
ortools

# Phase 3: LLM-based classification of free-text order descriptions
# (app/classification.py). Plain OpenAI SDK, function/tool-calling --
# no LangChain or agent framework, see the README for why.
openai

# Testing. httpx is what FastAPI's TestClient uses under the hood.
pytest
httpx
